{
  "name": "beamer-mono",
  "version": "1.0.0",
  "description": "Beamer Player platform - Digital signage and advertising solution",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm install && npm run build",
    "build:frontend": "cd cms-web && npm install && npm run build",
    "start": "cd backend && npm run start:migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/leaflet": "^1.9.21",
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0",
    "recharts": "^3.5.0"
  }
}
